spring:
  application:
    name: onboarding-backend-server

server:
  port: ${SERVICE_PORT:8080}   # HTTP (Tomcat) port

auth:
  service:
    client: "onboarding_product_catalogue_client"
    secret: "5yynnKSq4ERW23cJ"
    scope: "ONBOARDING"

onboarding:
  client:
      user: "onboarding"
      password: "mxTSms6aeQptCzCJ"
